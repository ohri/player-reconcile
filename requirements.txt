# Player Reconciliation Script Dependencies

# Data manipulation
pandas>=2.0.0

# HTTP requests for fetching NFLVerse data
requests>=2.31.0

# YAML configuration parsing
PyYAML>=6.0

# Oracle database connectivity
oracledb>=2.0.0
# Note: oracledb (python-oracledb) is the modern replacement for cx_Oracle
# It works in "thin" mode without requiring Oracle Instant Client
# For thick mode (Oracle Client features), see: https://python-oracledb.readthedocs.io/
